import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useEditor } from '../../state/EditorContext.jsx'
import './editor-login.css'

export default function EditorLogin() { const { editors, invitations, loginEditor } = useEditor(); const navigate = useNavigate(); const [email, setEmail] = useState(''); const [error, setError] = useState(''); return <div className="editor-invite-page"><div className="editor-invite-card"><span className="editor-invite-mark">✦</span><span className="editor-eyebrow">EDITOR SIGN IN</span><h1>Return to your workspace</h1><p>Use the email address from your accepted invitation.</p><input value={email} onChange={(event) => setEmail(event.target.value)} placeholder="editor@example.com" type="email" />{error && <p className="editor-error">{error}</p>}<button onClick={() => { const normalized = email.trim().toLowerCase(); const editor = editors.find((item) => item.email === normalized); if (editor) { if (editor.status !== 'active') return setError(`This editor account is ${editor.status}.`); loginEditor(editor); navigate('/editor/dashboard', { replace: true }); return } const invitation = invitations.find((item) => item.email === normalized); if (invitation) return setError(`This invitation is ${invitation.status}. Accept the invitation before signing in.`); setError('No editor or assistant access was found for this email.') }}>Continue</button></div></div> }
