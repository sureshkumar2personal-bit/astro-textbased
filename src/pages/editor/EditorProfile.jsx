import { useAuth } from '../../state/AuthContext.jsx'
import { useEditor } from '../../state/EditorContext.jsx'
import './editor-profile.css'
export default function EditorProfile() { const { currentUser } = useAuth(); const { recordAudit } = useEditor(); return <section className="editor-page"><div className="editor-page-intro"><span className="editor-eyebrow">ASSIGNED ACCESS</span><h2>My profile</h2><p>Your identity and access are managed by the astrologer.</p></div><div className="editor-panel editor-profile-card"><div className="editor-avatar">{currentUser?.name?.slice(0, 1)}</div><div><h3>{currentUser?.name}</h3><p>{currentUser?.email}</p><span className="editor-badge">{currentUser?.editorRole || 'Editor'}</span></div></div><div className="editor-panel"><h3>Permissions</h3><div className="editor-permission-list">{currentUser?.permissions?.map((permission) => <span key={permission}>{permission.replace('-', ' · ')}</span>)}</div><button className="editor-outline-button" onClick={() => recordAudit('Profile viewed', 'Editor opened assigned profile access.')}>Record profile review</button></div></section> }
